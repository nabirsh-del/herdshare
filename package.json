{
  "name": "herdshare",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "build:web": "npm run db:generate && turbo run build --filter=@herdshare/web",
    "lint": "turbo run lint",
    "db:generate": "cd packages/db && DATABASE_URL=\"postgresql://dummy:dummy@localhost:5432/dummy\" npx prisma generate",
    "db:push": "turbo run db:push --filter=@herdshare/db",
    "db:migrate": "turbo run db:migrate --filter=@herdshare/db",
    "db:seed": "turbo run db:seed --filter=@herdshare/db",
    "db:studio": "turbo run db:studio --filter=@herdshare/db"
  },
  "devDependencies": {
    "turbo": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.0.0"
}
